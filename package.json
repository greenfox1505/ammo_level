{
  "name": "ammo_level",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "$(npm bin)/watchify src/main.js -o \"./bin.js\" -v --debug --standalone GameEngine",
    "build": "$(npm bin)/browserify src/main.js -o \"./bin.js\" -v --standalone GameEngine"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "oimo": "^1.0.9",
    "three": "^0.87.1"
  },
  "devDependencies": {
    "watchify": "^3.9.0"
  }
}
